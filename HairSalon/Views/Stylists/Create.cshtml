@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<div class="jumbotron">
  <h3>Add a New Stylist</h3>
  <hr>
</div>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name, new { @class = "form-control", @required = "required" })

  @Html.LabelFor(model => model.Client)
  @Html.DropDownList("ClientId", null, new { @class = "form-control" })

  <div class="form-group">
    @Html.LabelFor(model => model.StylistLevel)
    @Html.DropDownList("StylistLevel", new List<SelectListItem> {
      new SelectListItem { Text = "Level 1", Value = "🟣"},
      new SelectListItem { Text = "Level 2", Value = "🟣🟣"},
      new SelectListItem { Text = "Level 3", Value = "🟣🟣🟣"},
      new SelectListItem { Text = "Level 4", Value = "🟣🟣🟣🟣"},
      new SelectListItem { Text = "Level 5", Value = "🟣🟣🟣🟣🟣"}, 
      }, new { @class = "form-control"})
  </div>

  <br>
  <input type="submit" value="Add New Stylist" class="btn btn-light"/>
}

<p class="btn btn-light">@Html.ActionLink("Show All Stylists", "Index")</p>
<p class="btn btn-light">@Html.ActionLink("Show all Clients", "Index", "Clients")</p>